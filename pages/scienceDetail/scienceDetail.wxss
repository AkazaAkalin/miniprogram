/* pages/scienceDetail/scienceDetail.wxss */
.wxParse-img{
  display: flex;
}
.title {
  margin: 0 1rem;
  font-size: 48rpx;
  text-align: center;
  font-weight: bold;
}
.shareBtn {
  padding: 0;
}
button{
  background-color:#ffffff; 
  border-radius: 0rpx;
  padding:0rpx;
  margin:0rpx;
  display: inline-block;
  line-height: 1;
  }

  button::after{ /*button的边框样式是通过::after方式实现的*/
  border:none;
  }
  
.button-hover{ /*点击后样式*/
  color:#000;
  background-color: rgb(255, 255, 255);
}
.shareContainer {
  position: fixed;
  bottom: 0;
  width: 100%;
  height: 150rpx;
  background-color: #ffffff;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 10rpx;
  border-top: 4rpx solid #999;
  padding-bottom: constant(safe-area-inset-bottom);
  padding-bottom: env(safe-area-inset-bottom);
}
.shareImg {
  width: 50rpx;
  height: 50rpx;
}